name: render pack

# S3.3: рендер-пак под тренд — Openverse CC-картинки + тренд-грейд → контакт-лист в 634.
# mimo НЕ нужен. Дёргает облачный бот /pack. Параметры — dispatch-inputs.

on:
  workflow_dispatch:
    inputs:
      grade_json:
        description: "JSON грейда {name,eq,balance}"
        required: false
        default: "{}"
      queries_json:
        description: "JSON-массив запросов"
        required: false
        default: "[]"

permissions:
  contents: write

jobs:
  pack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # was v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # was v5
        with:
          python-version: "3.11"
      - name: Deps
        run: |
          sudo apt-get update -qq && sudo apt-get install -y --fix-missing ffmpeg tesseract-ocr tesseract-ocr-rus
          pip install -q numpy pillow requests
      - name: Build pack
        env:
          OPENVERSE_CLIENT_ID:     ${{ secrets.OPENVERSE_CLIENT_ID }}
          OPENVERSE_CLIENT_SECRET: ${{ secrets.OPENVERSE_CLIENT_SECRET }}
          CLOUDFLARE_WORKER:       ${{ secrets.CLOUDFLARE_WORKER }}
          TELEGRAM_BOT_TOKEN:      ${{ secrets.TELEGRAM_BOT_TOKEN }}
          STYLE_SCOUT_CHAT_ID:     ${{ secrets.SCOUT_CHAT_ID }}
          STYLE_SCOUT_THREAD_ID:   "634"
          GRADE_JSON:   ${{ github.event.inputs.grade_json }}
          QUERIES_JSON: ${{ github.event.inputs.queries_json }}
        run: python3 render_pack.py

      - name: Persist pack manifest
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add packs/latest.json 2>/dev/null || true
          if git diff --cached --quiet; then echo "no manifest"; else
            git commit -m "pack: manifest [skip ci]"; git push
          fi
